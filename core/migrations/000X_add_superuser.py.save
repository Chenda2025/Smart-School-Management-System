
from django.db import migrations
from django.contrib.auth import get_user_model

def create_superuser(apps, schema_editor):
    User = get_user_model()
    if not User.objects.filter(username='admin').exists():  # change 'admin' to your username
        User.objects.create_superuser(
            username='admin',  # change to your username
            email='your@email.com',  # optional, change or ''
            password='yourpassword123'  # change to your real password
        )

class Migration(migrations.Migration):
    dependencies = [
        # Leave this as is (it will have the previous migration)
    ]

    operations = [
        migrations.RunPython(create_superuser),
    ]
